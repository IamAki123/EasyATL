# EasyATL Tuning (copy into TeamCode)

Full guide: [docs/Tuning.md](../docs/Tuning.md). First-time setup: [README](../README.md).

| Path | When |
| --- | --- |
| [Pedro](pedro/README.md) | You already use Pedro Pathing (`Follower`, `SelectableOpMode`) |
| [SDK / Road Runner](sdk/README.md) | No Pedro — VisionPortal tuner, optional `DefaultSdkConstants` |

Pedro files live under `tuning/pedro/` (`PedroEasyATLConstants`, `PedroEasyATLTuning`, `PedroPoseCorrector`). They are **not** in the EasyATL AAR.
